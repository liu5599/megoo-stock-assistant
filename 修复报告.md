# megoo股票助手 — 功能可用性修复报告

> 生成日期：2026-08-26
> 执行角色：高级软件功能测试工程师
> 结论：架构完整，但数据源链路大面积中断（SSL 证书 + 东财接口风控 + 多处 dict/list 混淆 + 财务字段错误），下游大量 `except: pass` 静默吞错，导致"功能点进去用不起"。本报告已修复全部核心阻断项。

---

## 一、缺陷清单与修复

### P0 — 核心阻断（不修则大面积功能空白）

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | akshare/东财/乐咕 全部 `SSL_CERTIFICATE_VERIFY_FAILED` | 本机有 SSL 拦截（自签名证书），代码又强制指向 certifi（不含代理根证书） | 新建 `utils/ssl_setup.py`，合并 certifi + macOS 系统钥匙串根证书到 `config/ca_bundle.pem`（192 个证书），`app.py`/`data/eastmoney_fetcher.py` 改调 `setup_ssl()` |
| 2 | 东财 clist 接口 `diff` 是 **dict**（键为索引），代码当 list 迭代 → 全市场/成分股/市值排名全返回 0 条 | 数据结构假设错误 | 新增 `EastMoneyFetcher._diff_list()` 兼容 dict/list，修复 7 处解析（`get_realtime_quote`/`get_all_stocks_spot`/`get_top_n_by_market_cap`/`get_index_components`/`get_stock_name`/`batch_get_financial_data`/`get_capital_flow`） |
| 3 | 选股"筛选条件"（PE/PB/ROE/营收/利润/毛利率/负债/市值/价格/行业）100% 失效 | `_apply_filters` 用 `fin.pe` 属性访问，但 `financial_data` 值是 **dict** → AttributeError 被 `except:pass` 吞掉 | `app/services/ranking_service.py` 改用 `fin.get()`，兼容 dict/对象；`data/data_utils.py` 补 `total_market_cap` 字段 |
| 4 | 批量财务返回 0 有效 | 请求 `fields` 缺 `f12`（代码字段），`code = item.get("f12")` 全空 | `batch_get_financial_data` 的 fields 补 `f12,f14` |

### P1 — 数据错误 / 结果失真

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 5 | 市值字段错误（茅台总市值显示 20） | `total_market_cap` 误用 `f115`（非市值字段） | 改用 `f20`（总市值，元），茅台正确返回 1.63 万亿 |
| 6 | 回测基本面因子空跑 | `signal_func` 里 `combiner.compute(snapshot, {})` 传空财务 dict | 接入真实 `fetch_financial_data`，并按当日 snapshot 对齐 |
| 7 | 东财历史K线接口 `push2his` 被服务器断开（Connection reset） | 东财风控 | `get_history_kline` 增加**腾讯 K线降级**（`web.ifzq.gtimg.cn`，字段顺序 date/open/close/high/low/volume） |
| 8 | `get_best_fetcher` 用失效的 K线接口检测 → 误判 EastMoney 不可用、回退 baostock | 检测接口选错 | 改用**实时行情接口**（`push2.eastmoney.com/api/qt/clist/get`）检测可用性 |

### P2 — 体验 / 一致性 / 可诊断性

| # | 问题 | 修复 |
|---|---|---|
| 9 | 移动端端口(8010) 与后端默认(8000) 不一致 | `app.py` 默认端口改为 **8010** |
| 10 | 个股详情页"长线决策"恒显示"数据不足"（取 400 天 < 70 周线需求） | 3 处 K线取数统一改为 **600 天**（`ops.py`/`daily_report_service.py`/`trade_planner.py`） |
| 11 | 市场情绪/涨跌家数/热度是硬编码假数据，界面看不出 | `MarketSentimentData` 加 `source`/`estimated` 字段，两 fetcher 标注，`market_service` 透传，`dashboard.html` 显示"⚠️估算值" |
| 12 | 个股资金流未实现 | `EastMoneyFetcher.get_capital_flow` 真实实现（东财 ulist 资金流字段，元→万元） |
| 13 | 大量 `except: pass` 静默吞错 | 关键数据源/搜索路径加 `logger.debug`（`stock.py`/两个 fetcher） |
| 14 | 全市场单页只返回 100 只 | `get_all_stocks_spot` 改为**分页拉取** + **退市股过滤**（名称含"退"或价≤0 跳过） |

---

## 二、修复文件清单（共 12 个）

| 文件 | 改动 |
|---|---|
| `utils/ssl_setup.py` | 新建：合并系统 CA + 幂等 setup |
| `config/ca_bundle.pem` | 生成：certifi + 系统根证书（192 个） |
| `app.py` | SSL 调用 + 默认端口 8010 |
| `data/eastmoney_fetcher.py` | K线腾讯降级、`_diff_list` 兼容、f12/f20、分页、资金流、异常日志 |
| `data/data_utils.py` | `get_best_fetcher` 检测、`total_market_cap` 字段 |
| `data/baostock_fetcher.py` | `estimated` 标记、异常日志 |
| `data/models.py` | `MarketSentimentData` 加 `source`/`estimated` |
| `app/services/ranking_service.py` | `_apply_filters` dict/属性修复 |
| `app/services/backtest_service.py` | 接入真实财务数据 |
| `app/services/market_service.py` | breadth 透传 `estimated`/`source` |
| `app/services/daily_report_service.py` | K线天数 600 |
| `app/routers/ops.py` / `app/routers/stock.py` | K线天数 600 / 搜索异常日志 |
| `analysis/trade_planner.py` | K线天数 600 |
| `app/templates/dashboard.html` | 估算标记 |

---

## 三、验证结果

- `py_compile`：全部通过
- 单元测试：**77 passed**（`test_factors`/`test_indicators`/`test_risk_manager`/`test_trade_planner`/`test_ops_modules`）
- `create_app()` 冒烟：44 条路由注册成功
- 筛选修复针对性验证：PE≤50 / ROE≥15 / 市值≥15000亿 三个条件过滤结果均正确

**真实数据拉取（修复前 → 修复后）**：

| 数据 | 修复前 | 修复后 |
|---|---|---|
| 乐咕市场活跃度 | SSL 错误 | ✅ 上涨2760/下跌2310/涨停56 |
| 涨停池 | SSL 错误 | ✅ 52 只 |
| 数据源选择 | baostock（被迫回退） | ✅ EastMoneyFetcher |
| 历史K线（茅台） | 仅 baostock | ✅ 40条，收盘1304（腾讯降级兜底） |
| 实时行情 | — | ✅ 茅台1302.8 / 平安11.73 |
| 全市场行情 | 0 只 | ✅ 分页拉取（数百只） |
| 批量财务 | 0 有效 | ✅ 茅台 PE18.29 / ROE16.75 / 市值1.63万亿 |
| 个股资金流 | 未实现 | ✅ 主力净流入7257万 / 净占比2.56% |

---

## 四、遗留问题与后续建议

1. **东财限频（临时）**：验证过程高频请求触发了东财 `push2.eastmoney.com` 风控，表现为返回空/断连。`get_all_stocks_spot` 已按东财标准参数配置（`fid=f6&po=0` 成交额降序）+ 分页 + 退市股过滤，但排序是否真正生效需等限频恢复后确认。**建议重启后端后间隔几分钟再请求**，避免短时间高频。
2. **全市场覆盖**：`get_all_stocks_spot` 默认分页 20 页（≈2000 只），未覆盖全部 5554 只。选股 `all` 池需要时可提高 `max_pages`，但会增加请求耗时与风控风险。
3. **baostock 财务字段补齐（增强项）**：baostock 兜底时仅返回 PE/PB，ROE/营收等仍缺失。当前 EastMoney 恢复后此路已非主路径，如需彻底补齐可接入 `query_profit_data`/`query_growth_data`。

---

## 五、重启验证步骤

```bash
cd /Users/liuuuo/megoo股票助手
./.venv/bin/python app.py          # 端口已统一为 8010
```

启动日志应出现：
```
🔐 SSL CA bundle 已就绪（含系统根证书，共 192 个）
✅ EastMoney 实时行情 API 可用
```

然后浏览器访问 `http://localhost:8010`，操盘台/题材/资金/选股排名等页面应能出真实数据。若公司代理根证书不在系统钥匙串中，需先：
```bash
export MEGOO_EXTRA_CA_BUNDLE=/path/to/公司根证书.pem
```
